import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  text: {
    paddingVertical: 10,
    fontWeight: '500',
  },
  textGold: {
    color: '#ef9c03',
  },
});

export default styles;
